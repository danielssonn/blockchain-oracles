async function main() {
  

  // 1. deploy StakingToken
  // 2. deploy RewardToken
  // 3. deploy Oracles
  // 4. deploy Award 

}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error)
    process.exit(1)
  })

